# Environment Variables for Dashboard

# Development: Use http://localhost:3000 automatically
# Production: Set at runtime via Docker/Kubernetes env vars

VITE_PUBLIC_POSTHOG_KEY=
VITE_PUBLIC_POSTHOG_HOST=https://eu.i.posthog.com
VITE_PUBLIC_API_URL=http://localhost:3000

# Note: In production, these are injected at runtime via the docker-entrypoint.sh script.
# Build-time placeholders are replaced with actual values when the container starts.
