{
  "name": "@repo/engine",
  "exports": {
    ".": "./index.ts",
    "./types": "./src/types.ts"
  },
  "scripts": {
    "lint": "biome check --write",
    "typecheck": "tsc --noEmit",
    "test:unit": "bun test --preload ./test-setup.ts src",
    "test:integration": "BUN_INTEGRATION_TESTS=1 bun test --timeout 60000 --concurrent --max-concurrency 4 src/services/*.integration.test.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "@repo/shared": "workspace:*",
    "@repo/tsconfig": "workspace:*",
    "zod": "catalog:"
  }
}
